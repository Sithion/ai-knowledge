import { ProvidersSection } from '../components/ProvidersSection.js';

/**
 * Dedicated top-level page for External Knowledge Providers — promoted out of
 * Settings since federated MCP search is a primary feature.
 */
export function ProvidersPage() {
  return <ProvidersSection />;
}
