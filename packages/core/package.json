{
  "name": "@cognistore/core",
  "version": "2.5.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc && rm -rf dist/db/migrations dist/db/seeds && cp -r src/db/migrations dist/db/migrations && cp -r src/db/seeds dist/db/seeds",
    "clean": "rm -rf dist",
    "test": "if find . -name '*.test.ts' -o -name '*.spec.ts' | grep -q .; then npx playwright test; else echo 'No tests found, skipping'; fi",
    "db:generate": "pnpm build && drizzle-kit generate",
    "db:push": "pnpm build && drizzle-kit push",
    "db:migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "@cognistore/shared": "workspace:*",
    "better-sqlite3": "^12.8.0",
    "drizzle-orm": "^0.45.0",
    "sqlite-vec": "^0.1.7",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/better-sqlite3": "^7.6.13",
    "drizzle-kit": "^0.31.0",
    "typescript": "^5.7.0"
  }
}
